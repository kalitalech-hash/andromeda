# Package manifest

Prepared as a ready-to-unzip candidate directory:

```text
andromeda_titles_plus_abstracts_pipeline/
```

## Contents

- `README.md` — operational overview and run commands.
- `requirements.txt` — Python dependencies.
- `config/default_config.json` — default reusable configuration.
- `andromeda_titles_plus_abstracts/` — shared helper functions.
- `scripts/` — executable pipeline stages.
- `templates/` — semantic map, stopwords, PEP-Web-style config template.
- `docs/` — methodology, output inventory, root README insert, PEP-Web methods note.
- `tests/` — small smoke-test fixture and runner.

## Public-repository caution

Do not commit restricted PEP-Web abstracts, full texts, PDFs, cookies, tokens, or credentials. Commit code, documentation, semantic maps, logs, and aggregate/derived analytical outputs only when rights status permits.
