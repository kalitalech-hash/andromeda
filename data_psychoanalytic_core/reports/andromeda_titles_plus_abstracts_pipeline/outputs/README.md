# outputs/

Runtime outputs are written here when the pipeline is run in-place. For applied corpora, prefer writing outputs inside the corpus workspace, not inside the reusable pipeline directory.

Do not commit restricted source abstracts or full-text material.
